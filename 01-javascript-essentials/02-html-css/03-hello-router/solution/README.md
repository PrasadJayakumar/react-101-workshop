# Assignment: URL Query String Handling

## Objective
Create a simple HTML page that accepts a URL with a query string, extracts the value of the `name` parameter, and displays it as the default value in a textbox.


